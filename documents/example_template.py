# RAG 문서 모듈 작성 예시 — 참고용 템플릿이다. documents/__init__.py의 REGISTRY에는
# 등록돼 있지 않고, 애초에 로딩 대상이 아니다. 2026-09-08 한때 documents/*.py(실제
# 페르소나·게임 콘텐츠 파일들)를 git에 커밋하지 않기로 했다가, Railway 배포가 git
# 저장소를 그대로 clone해서 실행하는 방식이라 그 파일들이 배포 환경에서 통째로
# 사라지는 문제를 뒤늦게 발견해 같은 날 롤백했다(CLAUDE.md §20 참고) — 이 파일은 그
# 시도의 유일한 잔재로, 새 RAG 문서를 추가할 때 형태만 참고하도록 남겨뒀다. 아래
# 코드는 전부 주석 처리돼 있어 실제로 import해도 아무 것도 정의되지 않는다 — 햄미가
# 실수로라도 이 파일을 문서로 읽거나 취급할 수 없게 하기 위한 의도적인 조치다.
#
# 새 RAG 문서를 추가하려면:
# 1. 이 파일처럼 `_TEXT` 상수와 `get_text()` 함수만 있는 모듈을 documents/ 아래에 만든다.
# 2. `documents/__init__.py`의 REGISTRY에 카테고리 이름 -> get_text 함수로 등록한다.
# 3. `core/intent.py`의 분류 카테고리 목록에도 그 이름을 추가해 자연어 라우팅이
#    그 문서를 찾을 수 있게 한다.
#
# _TEXT = """\
# 이것은 예시 문서입니다.
# """
#
#
# def get_text() -> str:
#     return _TEXT
