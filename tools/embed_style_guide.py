"""새 embed/임베드 텍스트를 작성하거나 기존 것을 고칠 때 참고하는 가독성·페르소나
가이드. 실행 코드가 아니라 참고 상수 — `tools/update_log_persona.py`(업데이트 로그
전용 가이드)와 동일한 역할을 embed 전반으로 넓힌 문서다. 2026-09-11, 승부예측
"선수 정보"(command/horse_race.py::_roster_field_value)가 번호·품종·이름·설명을
구분자 두 개(":"와 "—")로 한 줄에 욱여넣어 10마리 전부가 알아보기 힘든 긴 한 줄씩
으로 보이던 걸 카드형(이름 줄 + 설명 줄)으로 고치면서, 같은 문제가 다른 곳에도
없는지 전체를 훑어 정리한 것이 이 문서의 출발점이다. 새로 embed를 추가하거나
기존 텍스트를 만질 때마다 이 GUIDE를 다시 확인해 일관된 스타일을 유지할 것."""

GUIDE = """\
- **페르소나 vs 정중체 — CLAUDE.md §22-4가 원본, 여기는 실무 체크리스트**: embed는
  시스템 요소라 햄미의 반말/느낌표 페르소나("~이야!!"/"~써!!")를 쓰지 않는다.
  기본은 정중체("~습니다"/"~합니다")지만, 이미 있는 중립적 평서형 서술("~다"로
  끝나는 사전/안내문 톤, 예: "텅 비어있다")은 페르소나가 아니므로 굳이 정중체로
  다시 고칠 필요 없다. 판단 기준은 "반말+느낌표로 말 거는 캐릭터 말투인가"이지
  "문장이 100% 정중체인가"가 아니다. 페르소나 말투(반말/느낌표/오타)는 embed
  **밖**의 `content=` 플레이버 텍스트(인트로 한 줄 풀 등)에만 쓴다.
- **한 줄에 서로 다른 종류의 정보를 구분자 2개 이상으로 욱여넣지 않는다** — 예를
  들어 "번호 : 품종 : 이름 — 설명" 처럼 콜론과 대시를 섞어 쓰면 어디까지가 이름이고
  어디부터가 설명인지 한눈에 안 들어온다. 항목이 "식별 정보(번호/이름/품종 등) +
  서술형 설명(문장)"의 조합이면 **카드형으로 줄을 나눈다**:
  ```
  1️⃣ **잠보** (정글리안 햄스터)
  평소엔 졸려 보이지만 출발 신호가 울리면 눈이 번쩍! 의외의 막판 스퍼트가 특기예요.
  ```
  식별 줄은 굵게(`**`)로 강조하고, 설명 줄은 바로 아래 평문으로 둔다. 카드가
  여러 개면 `"\\n\\n"`(빈 줄)으로 구분해 숨 쉴 틈을 준다 — `command/achievements.py
  ::_entry_lines`(이름 줄/설명 줄)와 `command/horse_race.py::_roster_field_value`
  (2026-09-11 정정)가 이 패턴의 기준 사례다.
- **반대로 "같은 구조가 반복되는 짧은 데이터 행"은 한 줄에 몰아써도 된다** — 심볼별
  확률표(`command/slot.py::probability_summary`), 랭킹 목록(`command/ranking.py`
  의 "1. 이름 — 42"), 확률형 간식 공개(`command/probability_info.py
  ::black_market_field_value`)처럼 매 행이 정확히 같은 필드 구성(이름 — 수치)이고
  각 필드가 짧으면, 한 줄 표가 오히려 스캔하기 더 쉽다 — 이런 곳까지 억지로
  카드형으로 늘리지 않는다. 구분 기준은 "서술형 문장이 섞여 있는가"다: 문장이
  섞이면 카드형, 순수 수치/키워드 나열이면 한 줄 표.
- **번호 표기는 텍스트("1번")가 아니라 숫자 이모지**(1️⃣2️⃣3️⃣…, 10은 🔟)로 통일한다
  — 이모지는 텍스트보다 크게 렌더링돼 항목을 한눈에 구분하기 쉽고, 버튼/현황
  표시 등 다른 곳과도 표기가 맞아떨어진다(`command/horse_race.py
  ::_LANE_NUMBER_EMOJI`, `command/slot.py::_ROW_NUMBER_EMOJI`). "1번째 줄"처럼
  숫자+"번(째)"를 텍스트로 적는 건 지양한다.
- **구분자는 줄 하나에 하나만**: 콜론(`:`)은 "라벨: 값" 관계에, 대시(`—`)는 "핵심
  — 부연"관계에, 가운뎃점(`·`)은 서로 독립적인 짧은 항목을 나열할 때(예: 날짜 ·
  방법) 쓴다. 이미 라벨:값 형태인 줄에 대시로 부연까지 덧붙이면 그 순간부터
  가독성이 떨어지기 시작한다 — 부연이 필요하면 괄호로 묶거나 아예 다음 줄로
  뺀다.
- **글머리 기호(`- `)는 서로 독립적인 사실을 나열할 때**(규칙 설명, 레벨별 혜택,
  오늘의 기록 등) 쓴다 — `command/level_info.py::_level_embed`,
  `command/horse_race.py::HORSE_RACE_RULE_TEXT`가 기준 사례. 카드형 목록(위 항목)
  에는 글머리 기호를 쓰지 않는다 — 이름 줄이 이미 시각적 구분 역할을 한다.
- **색상은 기존 4종 상수를 재사용**(`core/base.py`, CLAUDE.md §22-4) — 새 색을
  만들기 전에 이미 있는 걸로 충분한지 먼저 확인한다: `EMBED_COLOR`(개인 정보),
  `SYSTEM_EMBED_COLOR`(시스템 공지), `LIST_EMBED_COLOR`(카탈로그/순위),
  `VENDING_EMBED_COLOR`(자판기 도메인). 게임류는 `command/economy_common.py`의
  `GAMBLING_EMBED_COLOR`/`BET_RECEIPT_EMBED_COLOR`를 쓴다. 정말 새 도메인이라
  기존 4~6종 어디에도 안 맞을 때만 새로 만들고, 그때도 `python -m
  tools.list_embed_colors`로 중복 값이 생기지 않는지 먼저 확인한다.
- **슬래시 명령어는 항상 백틱**(`` `/명령어` ``, CLAUDE.md §13-B) — embed 설명문
  안에서 언급할 때 맨 텍스트로 적지 않는다.
- **타인 클릭 거절 문구·조사(을/를, 이/가) 처리는 `core/korean.py::josa()`를
  통일해서 쓴다** — 하드코딩하면 받침 유무에 따라 "도박로"처럼 어색한 조사가
  나온다(`command/economy_common.py::reject_if_wrong_user_with_cta`가 기준 사례).
- 예시:
  - "1️⃣ **잠보** (정글리안 햄스터)\\n평소엔 졸려 보이지만..." (O — 카드형,
    식별 줄 굵게 + 설명 줄 분리)
  - "1번 정글리안 햄스터 : 잠보 — 평소엔 졸려 보이지만..." (X — 구분자 2개,
    한 줄에 다 욱여넣음, 2026-09-11 이전 실제 사례)
  - "🎰 딸기 — 칸당 12.5% (배율 x2)" (O — 짧은 데이터 행, 한 줄 표 형식 그대로 유지)
  - "네, {name}님의 호감도를 +5 올려드렸어요!!" (X — embed 밖이면 몰라도 embed
    안이면 페르소나 말투라 부적절, "올려드렸습니다"가 맞음)
"""
